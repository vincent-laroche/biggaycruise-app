# Use a network-accessible local IP for a physical device, for example http://192.168.1.25:8790.
# The Worker must be started with its development-only fixture mode enabled.
EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:8790
