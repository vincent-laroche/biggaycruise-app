export function developmentScannerPage(apiBaseUrl: string): string {
  const escapedBaseUrl = apiBaseUrl.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"/><meta name="viewport" content="width=device-width,initial-scale=1"/><title>BGC Development Scanner</title><style>body{margin:0;background:#ecfffc;color:#0a1946;font:16px system-ui,-apple-system,sans-serif}.shell{max-width:680px;margin:0 auto;padding:28px}.warning{background:#ffe33b;border:2px solid #0a1946;border-radius:18px;padding:16px;font-weight:800}.card{margin-top:18px;background:#fff;border:2px solid #0a1946;border-radius:22px;padding:22px;box-shadow:5px 5px 0 #0a1946}h1{font-size:28px;line-height:1;margin:12px 0}p{line-height:1.5;color:#46557e}button{background:#f63d91;border:2px solid #0a1946;border-radius:12px;color:#fff;font-weight:800;padding:12px 14px;margin:5px 5px 5px 0;cursor:pointer}button.secondary{background:#10d8d2;color:#0a1946}textarea{box-sizing:border-box;width:100%;min-height:110px;border:2px solid #a8d8d5;border-radius:12px;padding:10px;margin-top:12px;font:12px ui-monospace,SFMono-Regular,monospace}#result{margin-top:16px;border-radius:14px;padding:14px;background:#f2f6ff;white-space:pre-wrap;font-weight:700}.approved{background:#d9fff1!important}.unpaid{background:#fff0f7!important}.expired,.invalid{background:#fff6e3!important}</style></head><body><main class="shell"><div class="warning">DEVELOPMENT FIXTURE ONLY · Never use this page or its tokens for BGC production operations.</div><section class="card"><p style="font-weight:900;letter-spacing:.08em;font-size:12px">BIG GAY CRUISE</p><h1>Staff QR validation sandbox</h1><p>Generate an invented QR credential, or paste a credential from the guest development app. This tests the approved, unpaid, expired, and invalid outcomes without accessing a real booking.</p><div><button data-scenario="paid">Generate paid pass</button><button data-scenario="unpaid" class="secondary">Generate unpaid pass</button><button data-scenario="expired" class="secondary">Generate expired pass</button></div><textarea id="token" aria-label="Development QR credential" placeholder="Development credential appears here"></textarea><button id="validate">Validate credential</button><div id="result">Waiting for a development credential.</div></section></main><script>const base=${JSON.stringify(escapedBaseUrl)};const token=document.getElementById('token');const result=document.getElementById('result');async function create(scenario){const response=await fetch(base+'/v1/development/qr',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({scenario})});const data=await response.json();token.value=data.token||'';result.className='';result.textContent=data.token?'Credential created. Select Validate credential.':data.message||'Could not create credential.'}async function validate(){const response=await fetch(base+'/v1/qr/validate',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token:token.value})});const data=await response.json();result.className=data.status||'invalid';result.textContent=(data.status||'invalid').toUpperCase()+'\n'+(data.message||'Could not validate credential.')+(data.guestLabel?'\n'+data.guestLabel:'')+(data.passLabel?'\n'+data.passLabel:'')}document.querySelectorAll('[data-scenario]').forEach((button)=>button.addEventListener('click',()=>create(button.dataset.scenario)));document.getElementById('validate').addEventListener('click',validate);</script></body></html>`;
}
